@model CaterFlow.Models.ViewModels.LoginViewModel
@{
    ViewData["Title"] = "Login";
}

<div class="auth-card">
    <h1>Login</h1>
    <p class="muted">Continue to your role-based dashboard.</p>

    <form asp-action="Login" method="post" class="form-grid">
        <div asp-validation-summary="ModelOnly" class="validation"></div>

        <label asp-for="Email"></label>
        <input asp-for="Email" />
        <span asp-validation-for="Email" class="validation"></span>

        <label asp-for="Password"></label>
        <input asp-for="Password" />
        <span asp-validation-for="Password" class="validation"></span>

        <button type="submit" class="primary-btn">Login</button>
    </form>

    <p class="muted">No account yet? <a asp-controller="Account" asp-action="Register">Register</a></p>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
